package de.uniba.wiai.dsg.ss12.isabel.test;


//import static org.junit.Assert.*;

public class XmlSchemaValidatorTest {

	/*
	 * @Test public void test() throws MalformedURLException {
	 * 
	 * URL schemaLocationURL = new URL(
	 * "http://docs.oasis-open.org/wsbpel/2.0/OS/process/executable/ws-bpel_executable.xsd"
	 * ); File schemaLocationFile = new File(
	 * "U:/bpelval-proj/tool/resources/ws-bpel_executable.xsd"); String
	 * documentLocation = "U:/bpelval-proj/tool/Testcases";
	 * 
	 * XmlSchemaValidator xmlValidator; xmlValidator = new
	 * XmlSchemaValidator(schemaLocationURL);
	 * 
	 * File docFile = new File(documentLocation); if (docFile.isDirectory()) {
	 * DirectoryScanner dirScanner = new DirectoryScanner(docFile, ".bpel");
	 * List<File> files = dirScanner.scan(); xmlValidator.validate(files); }
	 * else { xmlValidator.validate(docFile); }
	 * 
	 * }
	 */

}
